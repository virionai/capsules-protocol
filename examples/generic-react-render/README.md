# Generic React Render Example

Builds a capsule containing static HTML rendered from a small React
component at build time. The packed work product is plain HTML and can be
opened directly in a browser without installing dependencies.

No warranty: this example is illustrative only. It is not a production
template, compliance artifact, legal advice, security advice, or
operational guidance.

```sh
npm install
npm test
```
