# Generic Table And Graph Example

Builds a capsule containing a self-contained HTML work product with an
interactive table and inline graph. It uses only browser-native HTML,
CSS, SVG, and JavaScript.

No warranty: this example is illustrative only. It is not a production
template, compliance artifact, legal advice, security advice, or
operational guidance.

```sh
npm test
```
