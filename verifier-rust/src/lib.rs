//! Empty workspace-root crate. The real entry point is the
//! `capsule-verify` library at `crates/capsule-verify/`. This file exists
//! solely to host the integration tests under `tests/` (Cargo requires a
//! library or binary target on the package owning a `tests/` directory).
